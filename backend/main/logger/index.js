'use strict';

const logger = require('./Logger');
const loggerInfo = require('./LoggerInfo');

const classes = {
    logger: logger,
    loggerInfo: loggerInfo
};

module.exports = classes  //eslint-disable-line
